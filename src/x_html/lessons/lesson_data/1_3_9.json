{
  "title": "Algorithm Formula: Dynamic API Request URL Builder",
  "topic": "3.2 • ALGORITHM CHALLENGE",
  "description": "<strong>Real-World Algorithm: Dynamic API Request URL Builder</strong><br/>Search engines, web scrapers, and mobile apps dynamically assemble query URLs using exact string formulas to request data across the internet.<br/><br/><strong>The Concatenation Formula:</strong><br/><div class=\"code-demo-box\" style=\"margin: 8px 0; padding: 10px; background: #0d1117; color: #fde047; border: 2px solid #000; border-radius: 6px; font-family: monospace; font-size: 1.05rem;\">url = protocol + \"://\" + domain + \"/\" + endpoint + \"?q=\" + query</div><br/><strong>The Challenge:</strong><br/>The configuration parameters below are locked. In the editable section, write the formula using string concatenation (<code>+</code>) to construct the complete API request URL in the variable <code>url</code>.<br/><br/><em>Notice:</em> Python does not insert delimiters automatically. You must combine the variables with the exact literal separators (<code>\"://\"</code>, <code>\"/\"</code>, <code>\"?q=\"</code>).<br/><br/>Expected output: <code>https://api.learner.dev/search?q=python</code>",
  "locked_prefix": "# --- API CLIENT PARAMETERS (LOCKED) ---\nprotocol = \"https\"\ndomain = \"api.learner.dev\"\nendpoint = \"search\"\nquery = \"python\"\n\n# --- WRITE YOUR CONCATENATION FORMULA BELOW ---\n",
  "locked_suffix": "\n# --- VERIFICATION (LOCKED) ---\nprint(url)\n",
  "starter_code": "# --- API CLIENT PARAMETERS (LOCKED) ---\nprotocol = \"https\"\ndomain = \"api.learner.dev\"\nendpoint = \"search\"\nquery = \"python\"\n\n# --- WRITE YOUR CONCATENATION FORMULA BELOW ---\nurl = \n\n# --- VERIFICATION (LOCKED) ---\nprint(url)\n",
  "intended_output": "https://api.learner.dev/search?q=python",
  "ai_check": "api_url_builder_algorithm"
}