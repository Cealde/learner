{
  "title": "Algorithm: FIFO Task Queue Dispatcher",
  "topic": "7.1 • ALGORITHM CHALLENGE",
  "description": "<strong>Real-World Algorithm: FIFO Job Queue Dispatcher</strong><br/>Web servers and message brokers process background tasks in First-In-First-Out (FIFO) order using queues.<br/><br/><strong>The Challenge:</strong><br/>The task queue below is locked. In the editable section:<br/>1. Append <code>\"SendEmail\"</code> to <code>queue</code>.<br/>2. Pop the first task from index 0 into variable <code>active_task = queue.pop(0)</code>.",
  "locked_prefix": "# --- BACKGROUND JOB QUEUE (LOCKED) ---\nqueue = [\"ProcessImage\", \"GeneratePDF\"]\n\n# --- WRITE YOUR QUEUE OPERATIONS BELOW ---\n",
  "locked_suffix": "\n# --- VERIFY DISPATCHED TASK (LOCKED) ---\nprint(active_task)\nprint(queue)\n",
  "starter_code": "# --- BACKGROUND JOB QUEUE (LOCKED) ---\nqueue = [\"ProcessImage\", \"GeneratePDF\"]\n\n# --- WRITE YOUR QUEUE OPERATIONS BELOW ---\n\n\n# --- VERIFY DISPATCHED TASK (LOCKED) ---\nprint(active_task)\nprint(queue)\n",
  "intended_output": "ProcessImage\n['GeneratePDF', 'SendEmail']",
  "ai_check": "fifo_queue_algorithm"
}